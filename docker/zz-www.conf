[www]
; Pipeline AI generation can make multiple sequential API calls
; Max wall-clock time before PHP-FPM kills the worker
request_terminate_timeout = 300s
; Allow more concurrent requests during batch operations
pm.max_children = 10
